$(document).ready(function() {
	/*$("#tabs").tabs({
		ajaxOptions: {
			error: function(xhr, status, index, anchor) {
				$(anchor.hash).html("Couldn't load this tab.");
			}
		},
		cache: true
	});*/
	
	//$(".portlet").addClass("ui-widget ui-widget-content");
	//$("#body_content").load("/tabs/network_status");
});